# Collector side: receive syslog over UDP 514 and file it by source host.
module(load="imudp")
input(type="imudp" port="514")

template(name="PerHost" type="string"
         string="/var/log/lab/%HOSTNAME%.log")

*.* ?PerHost
