# ulogd: read netfilter NFLOG group 1 (where the nft "log group 1" denies go)
# and emit each event to syslog, which the firewall's rsyslog forwards to the
# collector. This is the container friendly path: NFLOG is delivered over
# netlink, so it does not depend on kernel ring buffer access.
[global]
logfile="/var/log/ulogd.log"
loglevel=5

plugin="/usr/lib/ulogd/ulogd_inppkt_NFLOG.so"
plugin="/usr/lib/ulogd/ulogd_raw2packet_BASE.so"
plugin="/usr/lib/ulogd/ulogd_filter_IFINDEX.so"
plugin="/usr/lib/ulogd/ulogd_filter_IP2STR.so"
plugin="/usr/lib/ulogd/ulogd_filter_PRINTPKT.so"
plugin="/usr/lib/ulogd/ulogd_output_SYSLOG.so"

stack=log1:NFLOG,base1:BASE,ifi1:IFINDEX,ip2str1:IP2STR,print1:PRINTPKT,sys1:SYSLOG

[log1]
group=1

[sys1]
facility=LOG_LOCAL0
level=LOG_NOTICE
